<UserControl x:Class="SimpleHardwareMonitorGUI.Setting.Child.SettingWindowHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimpleHardwareMonitorGUI.Setting.Child"
             xmlns:customControl="clr-namespace:SimpleOverlayTheme.CustomControl;assembly=SimpleOverlayTheme"
             mc:Ignorable="d" 
             d:Width="800">
    <UserControl.Style>
        <StaticResource ResourceKey="SimpleOverlayTheme.UserControl.Default"/>
    </UserControl.Style>
    <UserControl.Content>
        <Grid Background="#60808080">
            <customControl:WindowHeader>
                <customControl:WindowHeader.TitleContent>
                    <Label Content="Setting" 
                           FontSize="24" Margin="0,0,0,0" Background="Transparent"
                           Style="{StaticResource SimpleOverlayTheme.Label.Default}"/>
                </customControl:WindowHeader.TitleContent>
                <customControl:WindowHeader.ShowMinimize>Collapsed
                </customControl:WindowHeader.ShowMinimize>
                <customControl:WindowHeader.ShowToggleMaximizeRestore>Collapsed
                </customControl:WindowHeader.ShowToggleMaximizeRestore>
            </customControl:WindowHeader>
        </Grid>
    </UserControl.Content>
</UserControl>
