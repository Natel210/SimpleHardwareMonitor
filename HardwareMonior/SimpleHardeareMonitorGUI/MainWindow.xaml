<Window x:Class="SimpleHardwareMonitorGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleHardwareMonitorGUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Closed="Window_Closed">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="text1" d:Text="1" Text="{Binding CpuUse, StringFormat={}{0:F2}}" Grid.Row="0"/>
        <TextBlock x:Name="text2" d:Text="2" Text="{Binding CpuVoltage, StringFormat={}{0:F2}}" Grid.Row="1"/>
        <TextBlock x:Name="text3" d:Text="3" Text="{Binding CpuPower, StringFormat={}{0:F2}}" Grid.Row="2"/>
        <TextBlock x:Name="text4" d:Text="4" Text="{Binding CpuTemperature, StringFormat={}{0:F2}}" Grid.Row="3"/>
    </Grid>
</Window>