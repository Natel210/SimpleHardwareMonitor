<Window x:Class="SimpleHardwareMonitorGUI.Setting.SettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleHardwareMonitorGUI.Setting"
        xmlns:theme="clr-namespace:GrayThemeUI;assembly=GrayThemeUI"
        xmlns:customControl="clr-namespace:GrayThemeUI.CustomControl;assembly=GrayThemeUI"
        xmlns:rawdata="clr-namespace:SimpleHardwareMonitorGUI.rawdata"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="SettingWindow"
        WindowStyle="None"
        Width="{StaticResource WindowWidth}" MinWidth="{StaticResource WindowWidth}" MaxWidth="{StaticResource WindowWidth}"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        Style="{StaticResource GrayThemeUI.Window}">
    <Window.Resources>
        <ObjectDataProvider x:Key="ERawDataIntervalProvider" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="rawdata:ERawDataInterval"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>
    <StackPanel Orientation="Vertical">
        <customControl:WindowHeader Title=" Setting" BorderBrush="{StaticResource GrayThemeUI.Overlay.Background.Focus}" BorderThickness="1" Margin="0,0,0,5" ShowMinimize="Collapsed" ShowToggleMaximizeRestore="Collapsed"/>
        <TextBlock Text="1123"/>
        <Button Content="123" Style="{StaticResource GrayThemeUI.SquareButton}"/>
        
        
        <ComboBox SelectedItem="{Binding SettingWindowLogging, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:SettingWindow}}}"
                  ItemsSource="{Binding Source={StaticResource ERawDataIntervalProvider}}"/>
        <Grid Width="300" Height="150">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button Content="22" Grid.Row="0" Style="{StaticResource GrayThemeUI.SquareButton}"/>
            <Button Content="33" Grid.Row="1" Style="{StaticResource GrayThemeUI.SquareButton}"/>
        </Grid>

    </StackPanel>
</Window>
